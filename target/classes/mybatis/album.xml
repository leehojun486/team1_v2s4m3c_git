<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<!-- dev.mvc.music.MusicDAOInter 패키지에 등록된 interface 명시,
      패키지명과 인터페이스명은 실제로 존재해야함,
      Spring이 내부적으로 자동으로 interface를 구현해줌. -->
<mapper namespace="dev.mvc.album.AlbumDAOInter">

  <insert id="create" parameterType="AlbumVO">
    INSERT INTO album(albumno, title, artist, genreno ,genre, listno, music)
    VALUES (album_seq.nextval, #{title}, #{artist}, #{genreno}, #{genre}, #{listno}, #{music})
  </insert>
  
  <select id="list" resultType="AlbumVO">
    SELECT albumno, title, artist, genreno ,genre, listno, music
    FROM album 
    ORDER BY albumno ASC
  </select>
  
  <select id="read" resultType="AlbumVO" parameterType="int">
    SELECT albumno, title, artist, genreno ,genre, listno, music
    FROM album 
    WHERE albumno = #{albumno}
  </select> 
  
  

</mapper>





